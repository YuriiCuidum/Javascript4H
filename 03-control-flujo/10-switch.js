let accion = 'listar';

switch (accion) {
    case 'listar':
        console.log('Accion de listar');
    case 'guardar':
        console.log('Accion de guardar');
        break;
    default:
        console.log('Accion no reconocido');  
}
if (accion == 'listar');
